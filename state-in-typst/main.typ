#import "./tool.typ":  sentence, init

The first sentence with parameter is: *"#sentence"*

We now change the parameter to 2024 with init function `#init(param: 2024)`

#init(param: 2024)

Then the new sentence with new parameter is: *"#sentence"*


